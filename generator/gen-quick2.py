import numpy as np
import generating as generating
from generating import GenerateRandom, GenerateRandomDec, GenerateRandomInc

def Generate_Quick2_Best(array_size, seed=42):
    return GenerateRandom(array_size, seed)

def Generate_Quick2_Worst(array_size, seed=42):
    return GenerateRandom(array_size, seed)

def Generate_Quick2_Average(array_size, seed=42):
    return GenerateRandom(array_size, seed)