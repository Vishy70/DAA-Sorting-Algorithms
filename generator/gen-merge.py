import numpy as np
import generating as generating
from generating import GenerateRandom, GenerateRandomDec, GenerateRandomInc

def Generate_Merge_Best(array_size, seed=42):
    return GenerateRandom(array_size, seed)

def Generate_Merge_Worst(array_size, seed=42):
    return GenerateRandom(array_size, seed)

def Generate_Merge_Average(array_size, seed=42):
    return GenerateRandom(array_size, seed)